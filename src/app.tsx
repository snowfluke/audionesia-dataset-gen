import type { JSX } from "@solidjs/web";

export default function App(): JSX.Element {
  return (
    <main class="p-6 text-sm text-kumo-default">
      <h1 class="text-lg font-semibold">Audionesia</h1>
      <p>Pembuat dataset suara untuk TTS bahasa Indonesia.</p>
    </main>
  );
}
